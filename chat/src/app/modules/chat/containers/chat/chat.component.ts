import { Component } from '@angular/core';

@Component({
  selector: 'chat',
  styleUrls: ['chat.component.scss'],
  template: `
    <div>
      chat
    </div>
  `
})

export class ChatComponent {
  constructor() {}
}
